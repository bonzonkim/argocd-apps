apiVersion: v1
kind: ServiceAccount
metadata:
  name: running-mate-postgresql
  namespace: running-mate
  labels:
    app.kubernetes.io/name: running-mate-postgresql
# The CNPG instance manager uses the Kubernetes API during initdb. A legacy
# chart left this same-named account with token mounting disabled.
automountServiceAccountToken: true
