apiVersion: v1
kind: Service
metadata:
  name: phonelink-frontend-service
  namespace: phonelink
  labels:
    app: ph-frontend
spec:
  ports:
    - port: 80
      targetPort: 80
  selector:
    app: ph-frontend
  type: ClusterIP
