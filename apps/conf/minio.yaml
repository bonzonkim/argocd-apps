auth:
  rootUser: admin
  rootPassword: root
ingress:
  enabled: true
  ingressClassName: traefik
  hostname: minio.k8s.bumgu.com
  extraTls:
    - hosts:
      - minio.k8s.bumgu.com
      secretName: k8s-bumgu-com-tls
 
tls:
  enabled: true
  existingSecret: k8s-bumgu-com-tls
